# ERP Desktop — development namuna
# Nusxa: bu faylni .env deb saqlang

# Dev (Vite proxy bilan — tavsiya):
VITE_API_URL=/api/v1

# Yoki proxy siz to'g'ridan-to'g'ri backend:
# VITE_API_URL=http://localhost:3000/api/v1

# Haqiqiy API (production va kundalik ish uchun):
VITE_USE_MOCK=false
