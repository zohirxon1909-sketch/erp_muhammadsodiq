/** No-op: backend is source of truth for prices and debt USD values. */
export function applyExchangeRate(_rate: number): void {
  // intentionally empty
}

export function syncAllWithActiveRate(): void {
  // intentionally empty
}
